SOURCES='package.json generic-data-server.d.ts'
TARGET=node_modules/@sabbatical/generic-data-server
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./generic-data-server.js ${TARGET} 
tsc -p tsconfig.tests.json
